# M2.10 — Ciudad late v1

## Objetivo

Hacer que `city-service` reaccione a `partido.terminado` con metricas urbanas propias, persistidas e idempotentes.

## Resultado

- `city-service` escucha `partido.terminado`.
- La ciudad mantiene metricas por partida:
  - fan sentiment
  - ticket sales index
  - local economy index
  - stadium district land value
  - win streak
  - loss streak
  - last match processed
- Las victorias suben animo, demanda de entradas y economia local.
- Las derrotas bajan momentum.
- Las rachas positivas elevan el valor del suelo del distrito del estadio.
- Las rachas negativas enfrían la economia local y presionan el valor del suelo.
- El procesamiento es idempotente por `(game_id, match_id)` en `city_processed_matches`.
- `city-service` publica:
  - `ciudad.economia_cambio`
  - `ciudad.suelo_actualizado`
  - `city.patch`
- `gateway` escucha `city.patch` y lo reenvia por WebSocket.
- El frontend mantiene `CityClientState`, muestra animo/suelo/demanda y activa un pulso visual sobre el estadio cuando llega un cambio urbano.

## Archivos principales

- `services/city-service/internal/domain/city.go`
- `services/city-service/internal/persistence/store.go`
- `services/city-service/cmd/main.go`
- `db/migrations/011_create_city_metrics_tables.sql`
- `services/gateway/cmd/main.go`
- `frontend/src/types.ts`
- `frontend/src/features/new-game/hooks/useNewGameFlow.ts`
- `frontend/src/features/new-game/components/CeremonyPage.tsx`
- `frontend/src/features/new-game/newGame.css`
- `shared/events/city.md`

## Pruebas corridas

```bash
GOCACHE=/tmp/pulsecity-city-gocache go test ./...
GOCACHE=/tmp/pulsecity-gateway-gocache go test ./...
npm run build --prefix frontend
make test-go
make test-rust
make build
```
